export default function PaymentsSlide({ payments }) {
    return <section className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{payments.map((period) => <article className="rounded-xl border border-white/10 bg-[#0c1a28] p-5 text-white" key={period.id}><h2 className="font-bold">{period.from_month}–{period.to_month} {period.year}</h2><ul className="mt-3 space-y-2 text-sm text-gray-300">{period.items?.map((item) => <li className="flex justify-between" key={item.id}><span>{item.name}</span><span>{item.status}</span></li>)}</ul></article>)}{!payments.length && <p className="text-gray-500">No payment periods configured.</p>}</section>;
}
